export declare enum TraceLevel {
    OFF = "off",
    DEFAULT = "default",
    VERBOSE = "verbose"
}
export interface LwrSpan {
    traceId: string;
    spanId: string;
    parentSpanId?: string;
    setAttributes(attributes: Record<string, string | number | boolean | string[] | undefined>): void;
    end(endTime?: number): void;
}
interface TraceID {
    name: string;
    attributes?: Record<string, string | number | boolean>;
}
interface TraceOptions {
    startTime?: number;
}
/**
 *  Tracer - encapsulates open-telemetry tracing apis
 */
declare class Tracer {
    private traceLevel;
    startSpan(id: TraceID, options?: TraceOptions): LwrSpan;
    trace<T>(id: TraceID, fn: (span: LwrSpan) => T): T;
}
/**
 * Tracer Singleton
 */
export declare const getTracer: () => Tracer;
export {};
//# sourceMappingURL=tracer.d.ts.map