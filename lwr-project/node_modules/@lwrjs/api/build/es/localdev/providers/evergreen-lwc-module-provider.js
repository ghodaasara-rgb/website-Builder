import LWCModuleProvider from '@lwrjs/lwc-module-provider';
import { logger } from '@lwrjs/diagnostics';
export default class EvergreenLWCModuleProvider extends LWCModuleProvider {
    name = 'evergreen-lwc-module-provider';
    constructor(options = {}, context) {
        super(options, context);
    }
    async getModuleEntry(moduleId, runtimeParams) {
        let entry;
        try {
            entry = await super.getModuleEntry(moduleId, runtimeParams);
        }
        catch (error) {
            logger.error(`Couldn't resolve module '${moduleId.specifier}'.`, error);
            throw error;
        }
        if (!entry) {
            return undefined;
        }
        entry.version = '';
        return entry;
    }
}
//# sourceMappingURL=evergreen-lwc-module-provider.js.map